import { CalcasistenciaPipe } from './calcasistencia.pipe';

describe('CalcasistenciaPipe', () => {
  it('create an instance', () => {
    const pipe = new CalcasistenciaPipe();
    expect(pipe).toBeTruthy();
  });
});
